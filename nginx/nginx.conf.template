worker_processes auto;
rtmp_auto_push on;
rtmp_auto_push_reconnect 1s;
events {}
http {
    include       mime.types;
    default_type  application/octet-stream;

    # Cloudflare Real IP (IPv4 only)
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 104.16.0.0/13;
    set_real_ip_from 104.24.0.0/14;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 131.0.72.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    real_ip_header CF-Connecting-IP;

    server {
        listen 8081;

        location /stat {
            rtmp_stat all;
            rtmp_stat_stylesheet stat.xsl;
        }

        location /stat.xsl {
            root /usr/local/nginx/html;
        }

        location /chat.html {
            root /usr/local/nginx/html;
        }

        location / {
            root /usr/local/nginx/html;
            index index.html;
        }

        # Health check and callbacks
        location /health {
            proxy_pass http://127.0.0.1:8080/health;
        }
        
        # Proxy to validator for OAuth and title updates
        location ~ ^/(login|callback|update_title|auth_status) {
            proxy_pass http://127.0.0.1:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
rtmp {
    server {
        listen 1935;
        chunk_size ${CHUNK_SIZE};

        # Stability and Security Enhancements
        ping 30s;
        ping_timeout 10s;
        drop_idle_publisher 10s;
        deny play all; # Prevent unauthorized viewing

        # Horizontal Application
        application ${APP_NAME} {
            live on;
            record off;
            on_publish http://127.0.0.1:8080/validate?key=$name;
            
            #youtube
            #facebook
            #instagram
            #tiktok
            #kick
            #x
            #twitch
            #rtmp1
            #trovo
        }

        # Vertical Application
        application vertical {
            live on;
            record off;
            on_publish http://127.0.0.1:8080/validate?key=$name;

            #v_youtube
            #v_facebook
            #v_instagram
            #v_tiktok
            #v_kick
            #v_x
            #v_twitch
            #v_rtmp1
            #v_trovo
        }
    }
}
